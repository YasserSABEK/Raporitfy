---
phase: 02-projects
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - app/(tabs)/projects/new.tsx
  - app/(tabs)/projects/[id]/index.tsx
  - app/(tabs)/projects/[id]/settings.tsx
  - lib/validation/schemas.ts
autonomous: true
requirements: [PROJ-01, PROJ-03, PROJ-04]

must_haves:
  truths:
    - "User can fill in a form and create a new project"
    - "User can view a project detail screen with visits placeholder"
    - "User can configure email recipients for a project"
    - "User can delegate project access to another user"
    - "All forms validate input before submission"
  artifacts:
    - path: "app/(tabs)/projects/new.tsx"
      provides: "Create project form screen"
      contains: "useForm"
    - path: "app/(tabs)/projects/[id]/index.tsx"
      provides: "Project detail screen"
      contains: "useProject"
    - path: "app/(tabs)/projects/[id]/settings.tsx"
      provides: "Project settings: recipients + members"
      contains: "useProjectRecipients"
    - path: "lib/validation/schemas.ts"
      provides: "Zod validation schemas"
      exports: ["projectSchema", "recipientSchema"]
  key_links:
    - from: "app/(tabs)/projects/new.tsx"
      to: "lib/hooks/useProjects.ts"
      via: "useCreateProject mutation"
      pattern: "useCreateProject"
    - from: "app/(tabs)/projects/[id]/index.tsx"
      to: "lib/hooks/useProjects.ts"
      via: "useProject(id) query"
      pattern: "useProject"
    - from: "app/(tabs)/projects/[id]/settings.tsx"
      to: "lib/hooks/useProjects.ts"
      via: "useProjectRecipients + useProjectMembers"
      pattern: "useProjectRecipients"
    - from: "new.tsx"
      to: "lib/validation/schemas.ts"
      via: "zodResolver(projectSchema)"
      pattern: "zodResolver"
---

<objective>
Build the create project form, project detail screen, and project settings screen (recipients + members).

Purpose: Complete the project management flow so users can create projects, view them, and configure who receives reports and who has access.
Output: Full project lifecycle in the app.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-projects/02-CONTEXT.md
@lib/hooks/useProjects.ts
@lib/services/projects.ts
@lib/theme.ts
@lib/types/domain.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Zod schemas + project creation form</name>
  <files>lib/validation/schemas.ts, app/(tabs)/projects/new.tsx</files>
  <read_first>
    - lib/types/domain.ts
    - lib/hooks/useProjects.ts
    - lib/theme.ts
    - .planning/phases/02-projects/02-CONTEXT.md
  </read_first>
  <action>
1. Create `lib/validation/schemas.ts`:
   ```typescript
   import { z } from 'zod';

   export const projectSchema = z.object({
     name: z.string().min(2, 'Le nom doit contenir au moins 2 caractères'),
     address: z.string().optional(),
     description: z.string().optional(),
     phase: z.string().optional(),
   });
   export type ProjectFormData = z.infer<typeof projectSchema>;

   export const recipientSchema = z.object({
     email: z.string().email('Adresse email invalide'),
     name: z.string().optional(),
   });
   export type RecipientFormData = z.infer<typeof recipientSchema>;
   ```

2. Create `app/(tabs)/projects/new.tsx`:
   - Use `react-hook-form` with `zodResolver(projectSchema)`
   - Fields:
     - "Nom du chantier" (required) — TextInput
     - "Adresse" (optional) — TextInput
     - "Phase chantier" (optional) — TextInput or simple picker
     - "Description" (optional) — TextInput multiline
   - "Créer le chantier" submit button (amber, 48px)
   - On success: navigate back to dashboard, show success feedback
   - Loading state on submit (disables button)
   - Error display per field (red text below input)
   - Header: "Nouveau Chantier" with back button
   - French labels, dark theme
  </action>
  <acceptance_criteria>
    - `lib/validation/schemas.ts` contains `z.object`
    - `lib/validation/schemas.ts` exports `projectSchema` and `recipientSchema`
    - `app/(tabs)/projects/new.tsx` contains `useForm`
    - `app/(tabs)/projects/new.tsx` contains `zodResolver`
    - `app/(tabs)/projects/new.tsx` contains `useCreateProject`
    - `app/(tabs)/projects/new.tsx` contains `Nom du chantier`
    - `app/(tabs)/projects/new.tsx` contains `Créer le chantier`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/yassersabek/Downloads/Raporitfy && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Create project form with Zod validation, react-hook-form, and French labels.</done>
</task>

<task type="auto">
  <name>Task 2: Build project detail + settings screens</name>
  <files>app/(tabs)/projects/[id]/index.tsx, app/(tabs)/projects/[id]/settings.tsx</files>
  <read_first>
    - lib/hooks/useProjects.ts
    - lib/validation/schemas.ts
    - lib/theme.ts
  </read_first>
  <action>
1. Create `app/(tabs)/projects/[id]/index.tsx`:
   - Get `id` from route params via `useLocalSearchParams`
   - Use `useProject(id)` to fetch project data
   - Show project header: name, address, phase, status badge
   - "Visites" section with placeholder: "Aucune visite — Phase 03"
   - "Réglages" button → navigate to `settings`
   - Loading/error states
   - Header shows project name

2. Create `app/(tabs)/projects/[id]/settings.tsx`:
   - Screen title: "Réglages du projet"
   - **Section: Destinataires**
     - List current recipients (email + name)
     - "Ajouter un destinataire" form inline
       - Email TextInput + Name TextInput + "Ajouter" button
       - Uses `recipientSchema` for validation
       - Uses `useAddRecipient()` mutation
     - Swipe-to-delete or delete icon on each recipient
     - Uses `useProjectRecipients(projectId)` and `useRemoveRecipient()`
   - **Section: Membres du projet**
     - List current members (name, email, role)
     - "Ajouter un membre" — email input to invite
     - Uses `useProjectMembers(projectId)` and `useAddMember()`
   - **Section: Actions**
     - "Archiver le projet" button (red/warning)
     - Uses `useArchiveProject()`
   - All French labels, dark theme
  </action>
  <acceptance_criteria>
    - `app/(tabs)/projects/[id]/index.tsx` contains `useProject`
    - `app/(tabs)/projects/[id]/index.tsx` contains `useLocalSearchParams`
    - `app/(tabs)/projects/[id]/settings.tsx` contains `useProjectRecipients`
    - `app/(tabs)/projects/[id]/settings.tsx` contains `useProjectMembers`
    - `app/(tabs)/projects/[id]/settings.tsx` contains `Destinataires`
    - `app/(tabs)/projects/[id]/settings.tsx` contains `Archiver le projet`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/yassersabek/Downloads/Raporitfy && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Project detail screen and settings screen with recipients management, member delegation, and archive button.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. Create project form validates inputs and submits to Supabase
3. Project detail shows fetched project data
4. Settings screen lists and manages recipients
5. Settings screen lists and manages members
6. Archive button works from settings
</verification>

<success_criteria>
- Create project form with Zod validation
- Project detail screen with header and visits placeholder
- Settings with recipients CRUD, members CRUD, archive
- All navigation wired: dashboard → detail → settings → back
- Forms show field-level errors in French
</success_criteria>

<output>
After completion, create `.planning/phases/02-projects/02-02-SUMMARY.md`
</output>
